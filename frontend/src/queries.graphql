query isAuthenticated {
  getAuthState(isAuthenticated: "true") {
    isAuthenticated
  }
}

query Name {
  getAuthState(isAuthenticated: "true") {
    idToken {
      name
    }
  }
}

query Version {
  version
}